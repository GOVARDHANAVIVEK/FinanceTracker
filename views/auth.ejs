<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signin & Signup Page</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
    <div class="window">
    <div class="header">
        <h2 class="title">Personal Expense Tracker</h2>
    </div>
    <div class="container">
        <div class="form-container">
            <div class="toggle">
                <button id="signin-btn" class="<%= activeForm === 'signin' ? 'active' : '' %>">Signin</button>
                <button id="signup-btn" class="<%= activeForm === 'signup' ? 'active' : '' %>">Signup</button>
            </div>
            <!-- Login Form -->
            <form id="signin-form" class="form <%= activeForm === 'signin' ? 'active' : '' %>" action="/dashboard" method="POST">
                <input type="text" name="username" placeholder="Username" >
                <input type="password" name="password" placeholder="Password" >
                <button type="submit" id="sendSignin">Signin</button>
                <span id="forgot-password"><a href="/forgot-password">Forgot password?</a></span>
            </form>
            
           
            
            <!-- Signup Form -->
            <form id="signup-form" class="form <%= activeForm === 'signup' ? 'active' : '' %>" action="/dashboard" method="POST">
                <input type="text" name="username" placeholder="Username" required>
                <input type="email" name="email" placeholder="Email" required>
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit" id="sendSignup">Signup</button>
            </form>
            <div id="error-message" style="color: red; display: none;"></div>
            
        </div>
        
    </div>
</div>
    <script src="index.js"></script>
</body>
</html>
